# ls-replacement source review (writer HX)

> 用途：记录 PARALLEL writer HX 在 2026-08-27 对 `eza`、`lsd` 两页做 STATIC_REVIEW 所用的固定源码输入。项目页是知识源真相；本文件只承担 review receipt provenance。Writer slug：`hx`。未占用开放或已合并 study 页的其他 writer slug。

## 范围与边界

- review date：2026-08-27
- writer：HX
- review_mode：`STATIC_REVIEW`
- evidence：GitHub metadata、crates.io 包元数据、固定提交静态源码阅读
- not executed：未安装两仓依赖，未编译上游二进制，未运行上游 test、bundle 或性能 benchmark
- worktrees：本机 `research-worktrees/`，不进入 Git

## eza

- canonical source：`https://github.com/eza-community/eza`
- revision：`98442ab17c2c3738701b62a7e060b1431ae2d6ea`
- package：`eza@0.23.5`（annotated tag `v0.23.5`）
- inspected：
  - `Cargo.toml`
  - `README.md`
  - `src/main.rs`
  - `src/lib.rs`
  - `src/options/mod.rs`
  - `src/options/view.rs`
  - `src/options/parser.rs`
  - `src/options/config.rs`
  - `src/options/filter.rs`
  - `src/fs/feature/git.rs`
  - `src/output/mod.rs`
  - `src/output/table.rs`
  - `src/output/render/git.rs`
  - `src/theme/mod.rs`
- observed：
  - `package.version` 为 `0.23.5`，`rust-version = "1.90"`，`edition = "2024"`，license `EUPL-1.2`；
  - default feature 含 `git`（可选依赖 `git2`）；无 `git` feature 时 `--git` / `--git-ignore` 会报错；
  - `main` 用 clap `get_command().get_matches()`，再 `Options::deduce`；无路径时默认 `"."`，也可从 stdin 按分隔符读路径；
  - TTY 且未指定布局时默认 `Mode::Grid`；非 TTY 默认 `Mode::Lines`；`--long` 走 `Details`，与 `--grid` 组合为 `GridDetails`；`--tree` 走带 recurse 的 `Details`；`--code` 抢先进入语言行数摘要，不列文件；
  - `Columns.git` 仅在 `--git` 且非 `--no-git`、且未设 `EZA_OVERRIDE_GIT` / `EXA_OVERRIDE_GIT` 时为 true；`should_scan_for_git` 还在 `--git-ignore` 时为 true；
  - `GitCache` 用向量缓存已发现仓库，首次查询才向 `git2` 取 statuses，之后复用；git 列渲染两格（staged + unstaged）：`- N M D R T I U`；
  - 默认主题文件位是 `dirs::config_dir()/eza/theme.yml`；颜色 Automatic 且非 TTY 时改用 plain UI。
- provenance note：
  - GitHub annotated tag `v0.23.5` 对象 `f7a9e054fb545f8f8bd36cf5c5d47fb36e5645d5`，peel 后指向 `98442ab17c2c3738701b62a7e060b1431ae2d6ea`；
  - tag 消息写的是 `auto generated by the justfile for eza v0.23.4`，但剥皮提交的 `Cargo.toml` 与 crates.io 稳定版都是 `0.23.5`；
  - 本文绑定 peel 后的 commit，不猜测 0.23.4 消息是否为 justfile 残留。

## lsd

- canonical source：`https://github.com/lsd-rs/lsd`
- revision：`d5a4e1cb80626d5ec94b237f6b77f7280d0f2fc9`
- package：`lsd@1.2.0`（lightweight tag `v1.2.0`）
- inspected：
  - `Cargo.toml`
  - `README.md`
  - `src/main.rs`
  - `src/core.rs`
  - `src/app.rs`
  - `src/config_file.rs`
  - `src/flags/blocks.rs`
  - `src/flags/color.rs`
  - `src/color.rs`
  - `src/icon.rs`
  - `src/theme.rs`
  - `src/theme/color.rs`
  - `src/theme/git.rs`
  - `src/git.rs`
  - `src/git_theme.rs`
  - `src/meta/git_file_status.rs`
- observed：
  - `package.version` 为 `1.2.0`，`rust-version = "1.85"`，`edition = "2024"`，license `Apache-2.0`；
  - default feature 含 `git2`；`no-git` 把 `GitCache` 编译成空实现；
  - `main` 用 `Cli::parse_from(wild::args_os())` 做 shell glob，再读 `config.yaml`/`config.yml`（或 `--config-file` / `--ignore-config`），`Flags::configure_from` 后交给 `Core`；
  - `Core::new`：非 TTY 且布局不是 Tree 时强制 `Layout::OneLine` 并 `literal=true`；`Core::run` 是 fetch → sort → display（grid 或 tree）；
  - 默认 long blocks 是 permission / user / group / size / date / name，不含 git；`--git` 且 `--long` 且非 `no-git` 才插入 `Block::GitStatus`；只有该 block 存在才 `GitCache::new`；
  - `GitCache` 用 `git2::Repository::discover` + `statuses(None)`；目录状态对子路径取 index/workdir 的 `max`；
  - 默认 git 符号是 `- . N ? D M R I T C`；
  - `config.yaml` 管行为；`color.theme = custom` 才读 `colors.yaml`；fancy 图标会尝试读 `icons.yaml`，没有则用内置 `IconTheme::default()`。
- provenance note：
  - GitHub tag `v1.2.0` 直接指向 `d5a4e1cb80626d5ec94b237f6b77f7280d0f2fc9`；
  - crates.io `lsd` 最新稳定版同为 `1.2.0`；
  - 本文绑定该 lightweight tag 对应 commit。
