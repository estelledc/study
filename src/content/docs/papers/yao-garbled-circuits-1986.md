---
title: Yao Garbled Circuits — 两个人不摊牌也能一起算答案
来源: 'Andrew C. Yao, "How to Generate and Exchange Secrets", FOCS 1986'
日期: 2026-07-07
分类: security-privacy
难度: 中级
---

## 是什么

Yao 这篇论文解决的是：**两个人各有秘密输入，想一起算一个函数，却不想把自己的秘密交出去**。日常类比：像两个人各拿半张拼图，只想知道拼出来是不是同一幅画，但不想把自己那半张给对方复印。

最有名的入口叫“百万富翁问题”：Alice 和 Bob 想知道谁更有钱，但谁都不想暴露具体资产。普通做法是找中介，Yao 的问题是：能不能只靠协议本身，做到“该知道的答案知道，不该知道的细节不知道”？

论文题目说的是“生成和交换秘密”，但它后来常被放进 **garbled circuits** 的源头脉络里看。原因是它把任意可计算任务看成协议要计算的函数，并说明可以在密码学假设下控制知识转移；后来的乱码电路把这个想法工程化成“把电路每根线的真假值换成随机标签”。

## 为什么重要

不理解这篇，下面这些事都很难解释：

- 为什么安全两方计算不是“加密后再解密”，而是把**计算过程本身**变成协议。
- 为什么隐私计算总说“只泄露输出”，因为输入之外的信息也可能被消息顺序和中间值泄露。
- 为什么公平性很难：一方先拿到结果后退出，另一方可能什么都得不到。
- 为什么 2PC、MPC、零知识证明、私有集合求交会共享一套词：参与方、视图、模拟、不可区分、恶意行为。

## 核心要点

1. **把任务写成函数**：先别问怎么加密，先问两个人到底要算 `f(i, j)` 和 `g(i, j)`。类比：做饭前先写菜单，否则锅碗瓢盆再高级也不知道要端出什么菜。

2. **隐私看的是“视图”**：协议不只检查最后答案，还检查每个人沿途看到的消息、随机数和中间结果。类比：考试只公布分数可以，不能把草稿纸也发出去。

3. **公平性是另一条约束**：隐私保证你少知道，公平性保证你不能先知道后跑路。类比：交换礼物不能一方先拆完盒子就关门。

## 实践案例

### 案例 1：百万富翁问题怎么抽象

```text
Alice 输入 i = 7
Bob   输入 j = 5

目标函数：
f(i, j) = 1  如果 i > j
f(i, j) = 0  如果 i <= j
```

**逐部分解释**：

- `i` 和 `j` 是私有输入，协议开始前只有各自知道。
- `f(i, j)` 是共同想知道的答案，不是两个输入本身。
- 安全协议的目标不是“让 Bob 永远不知道任何东西”，而是“Bob 只知道 `i > j` 这个输出能推出的东西”。

### 案例 2：乱码电路的最小直觉

```text
真实电路：AND(a, b)

线 a 的标签：a0, a1
线 b 的标签：b0, b1
输出标签：o0, o1

参与方只拿到 a1 和 b0，
能解出 o0，
但看不出 a1 代表 1 还是随机字符串。
```

**逐部分解释**：

- 每根线的 `0/1` 不直接出现，而是换成随机标签。
- 门表被打乱并加密，所以只能沿着自己手上的标签解一条路径。
- 输出标签再映射回真实答案，过程像“只给你能打开的一串保险箱钥匙”。

### 案例 3：交换秘密为什么难

```text
Alice 有秘密 SA：一个大数的分解
Bob   有秘密 SB：一个图的哈密顿回路

目标：
Bob 得到 SA 当且仅当 Alice 也能得到 SB
```

**逐部分解释**：

- 两个秘密可以完全不同类，不能简单按“字节数相等”交换。
- 论文关心的是知识转移的节奏：谁在什么时候已经能恢复什么。
- 公平协议要让“我拿到你的秘密，但你拿不到我的秘密”的概率变得可忽略。

## 踩过的坑

1. **把 1986 论文等同于现代乱码电路教程**：这篇是 extended abstract，重点是一般协议、秘密生成和公平交换，现代门级构造要看后续整理。

2. **以为加密消息就等于隐私**：消息被加密仍可能泄露长度、轮次、是否停止等信息，所以论文用视图和不可区分来约束。

3. **忽略公平性**：只做到“输入不泄露”还不够，一方可能在知道答案后中断，让另一方无法得到对应结果。

4. **把半诚实和恶意混在一起**：半诚实是假设照流程执行，恶意模型允许偏离流程；论文明确讨论了作弊和恢复算法，所以难度更高。

## 适用 vs 不适用场景

**适用**：

- 两方或多方各有私有数据，但只想公开某个函数结果。
- 需要解释 2PC、MPC、私有查询、秘密投票、隐私谈判等问题的共同骨架。
- 研究“只泄露输出”和“公平交换”这两个安全目标的区别。

**不适用**：

- 想找可直接复制的工程代码；这篇是理论 extended abstract，不是库文档。
- 需要抗量子或具体效率参数；论文主要基于大整数分解困难假设。
- 只关心通信加密；TLS 保护通道，安全计算保护“通道两端的人也互相不全信”。

## 历史小故事（可跳过）

- **1982 年**：Yao 在 “Protocols for Secure Computations” 里提出百万富翁问题，给安全计算立了一个清晰问题样板。
- **1986 年**：这篇 FOCS 论文把重点推进到秘密生成、秘密交换、隐私和公平性，并把一般两方计算纳入同一框架。
- **1987 年以后**：Goldreich、Micali、Wigderson 等工作把多方安全计算理论继续系统化，形成现代 MPC 语言。
- **2000 年**：Yao 获图灵奖，安全计算只是他贡献版图的一部分，但这条线深刻影响了密码学和隐私计算。

## 学到什么

1. **安全计算的核心不是藏答案，而是控制知识转移**：该输出的输出，不该多知道的别多知道。

2. **函数化是第一步**：把业务写成 `f(i, j)`，才有机会讨论协议是否正确、隐私、是否公平。

3. **公平性比隐私更难工程化**：因为它涉及中途退出、恢复、作弊检测，而不是单纯的消息保密。

4. **理论抽象会变成工程接口**：今天的 2PC 框架让开发者写电路或程序，背后仍是在回答 Yao 提出的那组问题。

## 延伸阅读

- 论文 PDF：[Yao 1986 — How to Generate and Exchange Secrets](https://mit6875.github.io/FA23HANDOUTS/yao-garbled-circuits.pdf)
- 早期问题来源：[Yao 1982 — Protocols for Secure Computations](https://research.cs.wisc.edu/areas/sec/yao1982-ocr.pdf)
- 注释书目：[Annotated Bibliography of Practical Secure Computation — Y86](https://web.engr.oregonstate.edu/~rosulekm/scbib/index.php?n=Paper.Y86)
- [[diffie-hellman-1976]] —— 一方向函数和公钥密码学为这类协议提供假设土壤
- [[goldreich-micali-wigderson-1987]] —— 合理预测会存在的 MPC 基础论文笔记
- [[oblivious-transfer-rabin-1981]] —— 合理预测会存在的“不知道也能转交选择”的基础原语

## 关联

- [[diffie-hellman-1976]] —— 提供“一方向函数 / 公钥”这类密码学假设的历史起点。
- [[rsa]] —— 大整数分解困难是假设背景之一，论文里的秘密示例也常用分解。
- [[cryptoverif-2008]] —— 后来的协议验证工具，关心的仍是“协议到底泄露了什么”。
- [[easycrypt-2011]] —— 把密码协议证明写成可检查脚本，延续形式化安全证明路线。
- [[zero-knowledge-proofs]] —— 合理预测会存在；同样追求“证明一件事但不多泄露”。
- [[secure-multiparty-computation]] —— 合理预测会存在；Yao 这条线的直接后代。

## 反向链接

<!-- 由 scripts/regen-backlinks.mjs 自动生成 -->
