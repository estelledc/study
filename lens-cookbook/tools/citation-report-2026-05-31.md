# Citation Meter Report — 2026-05-31

## Totals

- study/written.txt 总数：822
- lens 累计引用唯一 slug：81
- 覆盖率：9.85%
- priority-queue picked：30
- priority 实际被引用：22 / 30

### priority picked 但 lens 没引
- `github-actions`
- `orca-continuous-batching`
- `sarathi-serve`
- `distserve`
- `skip-locked-postgres-9.5`
- `mcp-spec`
- `openai-agents-sdk`
- `claude-agent-sdk`

## Per-lens

| lens | wikilinks | table 候选 | plaintext 命中 | 唯一引用 slug | dead wikilinks | table 未匹配 |
|---|---|---|---|---|---|---|
| aieng | 0 | 16 | 9 | 9 | 0 | 10 |
| backend | 0 | 21 | 24 | 26 | 0 | 3 |
| data | 0 | 18 | 14 | 16 | 0 | 7 |
| devops | 0 | 21 | 6 | 8 | 0 | 14 |
| frontend | 0 | 0 | 21 | 21 | 0 | 0 |
| vllm | 0 | 0 | 7 | 7 | 0 | 0 |

### lens-aieng

引用 slugs（9）：`anthropic-prompt-caching`, `autogen`, `claude-code`, `langchain`, `litellm-proxy`, `oauth-2.1-rfc`, `promptfoo`, `server-sent-events`, `vercel-ai`

table 候选但 study 无笔记：
- LangGraph
- OpenAI Agents SDK
- OpenRouter
- Portkey
- MCP SDK
- stdio
- Streamable HTTP
- outlines/xgrammar
- mem0/Letta
- LangSmith

### lens-backend

引用 slugs（26）：`axum`, `bullmq`, `celery`, `cockroachdb`, `drizzle`, `fastapi`, `fastify`, `hono`, `jwt-rfc-7519`, `kubernetes`, `kysely`, `lucia`, `mongodb`, `mysql`, `pg-boss-readme`, `pgvector`, `postgres-js`, `prisma`, `redis`, `rest-fielding-2000`, `server-sent-events`, `sqlite`, `token-bucket-stripe`, `trpc`, `typeorm`, `valkey`

table 候选但 study 无笔记：
- SQLAlchemy 2.x
- OAuth2
- GCRA

### lens-data

引用 slugs（16）：`ann-benchmarks`, `bm25-okapi`, `chroma`, `clickhouse`, `colbert-v2`, `dagster`, `diskann-2019`, `duckdb`, `hnsw-2018`, `milvus`, `pgvector`, `qdrant`, `rrf-cormack-2009`, `snapshot`, `snowflake`, `weaviate`

table 候选但 study 无笔记：
- dense embed
- hybrid RRF
- reranker
- IVF-PQ
- BigQuery
- Airflow
- Debezium

### lens-devops

引用 slugs（8）：`argocd`, `gh`, `kubernetes`, `opentelemetry`, `otel-collector`, `prom-client`, `sentry`, `terraform`

table 候选但 study 无笔记：
- Buildkite
- Jenkins
- Compose
- Nomad
- ECS
- Datadog
- Loki
- GPU Op
- Slurm
- Spot
- Karpenter
- CA
- OpenTofu
- Pulumi

### lens-frontend

引用 slugs（21）：`astro`, `conform`, `islands-architecture`, `jotai`, `playwright`, `qwik`, `react`, `react-server-components`, `remix`, `server-sent-events`, `styled-components`, `svelte`, `sveltekit`, `tailwind`, `tanstack-form`, `tanstack-query`, `vanilla-extract`, `vite`, `vitest`, `websocket-rfc-6455`, `zustand`

### lens-vllm

引用 slugs（7）：`awq`, `eagle`, `kubernetes`, `litellm-proxy`, `llama`, `sglang`, `vllm`

